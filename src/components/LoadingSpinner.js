import React from 'react';
import loadingSpinner from '../React-icon.svg';

const LoadingSpinner = () => {
  return <img src={loadingSpinner} alt="loading-spinner" className="loading-spinner"></img>;
}

export default LoadingSpinner;